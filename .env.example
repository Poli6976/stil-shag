# Скопируйте в .env.local для локальной разработки (vercel dev) —
# на самом Vercel эти же имена задаются в Settings → Environment Variables,
# .env.local туда не попадает и в репозиторий коммитить его не нужно.

# Authorization key из личного кабинета GigaChat API:
# developers.sber.ru → создать проект "GigaChat API" → Авторизационные данные
GIGACHAT_AUTH_KEY=

# GIGACHAT_API_PERS — для физлица/самозанятого, GIGACHAT_API_B2B — для юрлица.
# Уточняется при подключении тарифа в личном кабинете.
GIGACHAT_SCOPE=GIGACHAT_API_PERS

# Необязательно — какую модель GigaChat использовать. По умолчанию базовая
# GigaChat (дешевле всего), GigaChat-Max — точнее и дороже.
GIGACHAT_MODEL=GigaChat

# ---- Коды программы «Примерка» (api/generate-code.js, api/redeem-code.js) ----

# Секрет, которым подписываются коды (формула — lib/partnerCode.js). Любая
# длинная случайная строка, например сгенерированная командой:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Если он утечёт — кто угодно сможет подделывать валидные коды. Никогда не
# показывать, не публиковать, не использовать тот же секрет для ADMIN_KEY.
PARTNER_CODE_SECRET=

# Пароль для admin-generate-code.html — служебной страницы, где владелец
# сайта выдаёт коды. Тоже длинная случайная строка (сгенерировать так же,
# как выше), но ДРУГАЯ, не равная PARTNER_CODE_SECRET.
ADMIN_KEY=

# ---- Депозит и оплата (lib/wallet.js, api/payments/*, api/orders/*) ----
# Подробная инструкция подключения — DEPLOY-payments.md.

# Supabase-проект (supabase.com → New project). URL и ключи — в
# Project Settings → API. SUPABASE_ANON_KEY публичный по замыслу (уходит в
# браузер, защищён политиками RLS из db/schema.sql) — SUPABASE_SERVICE_ROLE_KEY
# наоборот, полный доступ в обход RLS, ему нельзя покидать переменные
# окружения сервера ни при каких обстоятельствах.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Платёжная форма Prodamus (prodamus.ru → личный кабинет → Настройки формы).
# PRODAMUS_SHOP_URL — адрес вашей формы вида https://ваш-магазин.payform.ru/
# PRODAMUS_SECRET_KEY — секретный ключ формы оттуда же, нужен только для
# проверки подписи вебхука (lib/payments/prodamus.js) — расчётный счёт для
# выплат указывается на стороне Prodamus при регистрации магазина, не здесь.
PRODAMUS_SHOP_URL=
PRODAMUS_SECRET_KEY=

# Необязательно — "1" включает тестовый режим Prodamus (платежи не списывают
# реальные деньги). Снять/убрать перед приёмом настоящих платежей.
PRODAMUS_DEMO_MODE=

# Необязательно — публичный адрес сайта для return_url после оплаты
# (например https://stil-tvoy-shag.ru). Если не задано, берётся из
# заголовка запроса — для большинства случаев этого достаточно.
SITE_URL=
