/* ============================================================================
   Клиент генерации картинки образа (Yandex Cloud AI Studio) — ОДНА
   иллюстративная картинка на весь образ по текстовому описанию слоёв.

   2026-09-10 — переход с модели YandexART (async-эндпоинт
   /foundationModels/v1/imageGenerationAsync) на Alice AI ART (sync,
   OpenAI-совместимый /v1/images/generations): YandexART в этот день стала
   стабильно падать с "Access to model art://.../yandex-art/latest denied"
   (модель в каталоге переименовалась в yandex-art-2.0 — это чинилось
   отдельно), а после починки имени модели асинхронная генерация начала
   зависать на много минут без ответа (проверено напрямую через API —
   операция не завершалась и через 10+ минут, хотя биллинг/права/квоты в
   порядке). Alice AI ART — синхронный запрос-ответ без опроса статуса,
   проверено напрямую: устойчиво отвечает за 5-6 секунд.

   Нужны два значения в переменных окружения (Vercel → Settings → Environment Variables):
     YANDEX_API_KEY     — API-ключ сервисного аккаунта (роль ai.imageGeneration.user)
     YANDEX_FOLDER_ID    — ID каталога в Yandex Cloud (видно в адресной строке консоли)
   ============================================================================ */

const https = require('https');

const API_HOST = 'ai.api.cloud.yandex.net';

/* Реальный лимит — 500 символов на текст промпта (тот же лимит указан в
   AI Studio и для YandexART, и для Alice AI ART; проверено раньше по ответу
   API: "Prompt positive size 702 exceeds limit (500)"). 460, а не ровно
   500 — небольшой запас на случай, если код обрежет что-то ровно на границе
   символа UTF-16 суррогатной пары. */
const PROMPT_MAX_LENGTH = 460;

function httpsJson(hostname, method, requestPath, headers, bodyBuffer) {
  return new Promise((resolve, reject) => {
    const req = https.request(
      { hostname, port: 443, path: requestPath, method, headers, timeout: 30000 },
      (res) => {
        var chunks = [];
        res.on('data', (c) => chunks.push(c));
        res.on('end', () => {
          var raw = Buffer.concat(chunks).toString('utf8');
          var parsed;
          try { parsed = raw ? JSON.parse(raw) : {}; } catch (e) { parsed = { raw: raw }; }
          resolve({ status: res.statusCode, body: parsed });
        });
      }
    );
    req.on('timeout', () => req.destroy(new Error('timeout')));
    req.on('error', reject);
    if (bodyBuffer) req.write(bodyBuffer);
    req.end();
  });
}

/* Возвращает base64 JPEG (без data:-префикса) готовой картинки образа.
   opts.size — необязательно, по умолчанию портретный кадр: квадратный кадр
   физически не оставляет места нарисовать фигуру в полный рост с обувью,
   модель вынуждена обрезать по бёдра/колено независимо от текста промпта.
   Портретный даёт вертикальный запас под голову-плечи-ноги-обувь
   одновременно (1024×1536 ≈ 2:3, проверено — реальный ответ модели пришёл
   832×1280, тот же портретный характер). */
async function generateLookImage(prompt, opts) {
  opts = opts || {};
  var folderId = process.env.YANDEX_FOLDER_ID;
  var payload = JSON.stringify({
    model: 'art://' + folderId + '/aliceai-image-art-3.0/latest',
    prompt: prompt.slice(0, PROMPT_MAX_LENGTH),
    size: opts.size || '1024x1536'
  });
  var headers = {
    'Authorization': 'Bearer ' + process.env.YANDEX_API_KEY,
    'Content-Type': 'application/json',
    'Content-Length': Buffer.byteLength(payload)
  };
  var res = await httpsJson(API_HOST, 'POST', '/v1/images/generations', headers, payload);
  var image = res.body && res.body.data && res.body.data[0] && res.body.data[0].b64_json;
  if (res.status !== 200 || !image) {
    throw new Error('Alice AI ART: генерация не удалась: ' + res.status + ' ' + JSON.stringify(res.body));
  }
  return image;
}

/* Голая цифра российского размера ("размер 64") ничего не значит для модели,
   рисующей картинки — она не обучена на таблицах размеров одежды и без явных
   слов про телосложение почти всегда рисует типовую худую модель, даже если
   в промпте есть сам номер размера. Переводим размер в описательную фразу ДО
   того, как он попадёт в промпт. Пороги — стандартная российская сетка
   женской/унисекс одежды (округлённо, т.к. fit — свободный текст
   пользователя, а не строгий выбор из списка). Используется и
   api/compose-look.js (фото), и api/looks/charge.js (визард-анкета) — общее
   место, чтобы не держать логику в двух копиях.

   Границы сдвинуты 2026-08-28: при размере 56 картинка получалась похожа на
   64-й (модель явно перевыполняет словосочетание "плюс-сайз" даже на нижней
   границе диапазона) — расширили менее нагруженную формулировку "полная
   фигура" до 56 включительно, а "плюс-сайз" сдвинули на 57+.

   Сдвинуты ещё раз 2026-09-13: та же самая перегрузка обнаружилась и на
   границе 62 — по фото клиентки на 62-й размер образ вышел заметно крупнее,
   чем сама клиентка (тот же эффект, что чинили выше, просто на следующей
   границе). "Полная фигура" расширена до 62 включительно, "плюс-сайз"
   сдвинут на 63+.

   Пересмотрено 2026-09-16 (решение пользователя): на размере 62 с мягкой
   формулировкой "полная фигура" YandexART дважды подряд нарисовал обычную
   стройную модель — то есть тот же самый порог 62 давал то перебор (см.
   выше), то полный игнор инструкции. Раз одной и той же фразой нельзя
   одновременно избежать обеих ошибок, важнее не какая из них — для клиентки
   с размером 62-64 увидеть на картинке модель размера 48 недопустимо
   (обесценивает весь смысл продукта для plus-size аудитории), а слегка
   преувеличенная фигура — не страшно. Поэтому пороги сдвинуты в пользу
   более сильной, однозначной формулировки раньше, а не позже: "полная
   фигура" теперь только до 54, "плюс-сайз" начинается с 55 (было с 63).

   Уточнено сразу же в той же сессии: первая версия добавила к фразам
   описательные детали ("широкие плечи, полное тело" и т.п.), не проверив,
   действительно ли они помогают — а бюджет промпта и без того тесный (см.
   PROMPT_MAX_LENGTH). Реальный тест сразу показал цену: "пиджак" в
   "Верхняя одежда" должен получать sleeveNote ("обязательно с длинными
   рукавами, не жилет") — но бюджета на неё не осталось именно из-за
   удлинённой фразы про фигуру, и картинка снова нарисовала жилет вместо
   пиджака. Проверил и на старом коде (до сегодняшних правок, коммит
   ed00697) — на этой же комбинации слоёв sleeveNote тоже не помещалась
   (бюджет и раньше был тесным для трёх непустых слоёв "Остальной образ" +
   пиджак/жакет), так что сам факт нехватки — не новая проблема сегодняшней
   сессии. Но раньше хотя бы dressNote успевала поместиться (более короткая
   подсказка) — с удлинённой фразой перестала помещаться и она. Вернул
   формулировки почти к исходной длине ("крупная фигура, плюс-сайз" — та
   самая, что уже давала рабочие результаты в истории кабинета пользователя
   до этой сессии), без непроверенных довесков типа "XXL"/описательных
   деталей — они стоили бюджета больше, чем показали пользы. */
function sizeToBodyPhrase(size) {
  if (size === null || size <= 46) return null; // близко к типовой модельной фигуре — уточнять нечего
  if (size <= 50) return 'плотнее среднего';
  if (size <= 54) return 'полная фигура, плюс-сайз';
  if (size <= 66) return 'крупная фигура, плюс-сайз';
  return 'очень крупная фигура, большой плюс-сайз';
}

/* Короткая версия фразы для повтора в конце промпта (см. tailNote ниже) —
   без уточняющих деталей в скобках, чтобы не удваивать длину. */
function bodyPhraseShort(phrase) {
  if (!phrase) return null;
  var idx = phrase.indexOf(':');
  return idx === -1 ? phrase : phrase.slice(0, idx);
}

function extractClothingSize(fit) {
  if (!fit) return null;
  var m = /размер\D{0,5}(\d{2})\b/i.exec(fit) || /\b(\d{2})\D{0,5}размер/i.exec(fit);
  if (m) return parseInt(m[1], 10);
  var nums = fit.match(/\b\d{2}\b/g); // рост обычно 3 цифры (140-200) — сюда не попадёт
  if (!nums) return null;
  for (var i = 0; i < nums.length; i++) {
    var n = parseInt(nums[i], 10);
    if (n >= 38 && n <= 72) return n;
  }
  return null;
}

function truncate(str, maxLen) {
  if (!str || str.length <= maxLen) return str;
  return str.slice(0, maxLen).trim() + '…';
}

/* js/wizard.js оборачивает вещь пользователя в "Уже есть — это ваша вещь:
   «...»" (+ "(выполняет роль верха и низа)" для платьев) — эта фраза нужна
   только для текста на экране. Для картинки она бесполезна и, что важнее,
   вредна: сама обёртка уже занимает ~27 символов, и при обрезке слоя до
   32 символов (см. ниже) от реального описания вещи оставалось буквально
   пара слов — картинка рисовалась почти вслепую и получалась ближе к
   шаблонным соседним слоям (юбка/жакет повода), чем к самой вещи. */
var ITEM_WRAPPER_RE = /^Уже есть — это ваша вещь: «(.*)»(?:\s*\([^)]*\))?$/;
function stripItemWrapper(str) {
  if (!str) return str;
  var m = ITEM_WRAPPER_RE.exec(str.trim());
  return m ? m[1] : str;
}

/* GigaChat иногда цитирует реальную надпись/принт с вещи прямо в описании слоя (например,
   'Белая футболка с надписью «Trend Shades color palette»') — SYSTEM_PROMPT это запрещает
   (см. api/compose-look.js), но на всякий случай подчищаем и здесь: любой цитируемый текст в
   кавычках уходит в промпт художнику как команда нарисовать именно эти буквы на картинке,
   а не описание кроя. Применяется ПОСЛЕ stripItemWrapper — обёртку js/wizard.js это не трогает,
   она уже развёрнута к этому моменту. */
var QUOTED_TEXT_RE = /[«"][^«»"]*[»"]/g;
function stripQuotedText(str) {
  if (!str) return str;
  var cleaned = str.replace(QUOTED_TEXT_RE, '').replace(/\s{2,}/g, ' ').trim();
  return cleaned || str; // если вырезали всё дотла — лучше исходный текст, чем пустота
}

/* В визарде (online-stylist.html) только ОДИН слой — реальная вещь
   пользователя (js/wizard.js оборачивает её в "Уже есть — это ваша вещь:
   «...»", см. ITEM_WRAPPER_RE выше), а остальные 4-5 слоёв — статичный
   шаблон повода ("Юбка-карандаш длины миди", "Туфли на каблуке" и т.п.),
   никак не связанный с реальной вещью. При равной обрезке (было — все слои
   по 50 символов) шаблонные слои численно перевешивали реальную вещь, и
   YandexART рисовал образ по шаблону повода (например «вечер» — приталенное
   платье/юбка-карандаш), а не по реальной вещи с фото — пользователь прислал
   пример: клетчатая рубашка с котом на кармане → картинка чёрно-белого
   платья, ничего общего. Даём реальной вещи больше символов и явный
   приоритет первой фразой промпта; в "Образе по фото" (api/compose-look.js)
   этот путь не задействуется — там ни один слой не обёрнут в ITEM_WRAPPER_RE
   (все слои одинаково реальные, из фото), и функция ведёт себя как раньше. */
var REAL_ITEM_MAX = 90;
var TEMPLATE_LAYER_MAX_WITH_ITEM = 36;
var TEMPLATE_LAYER_MAX_ALONE = 50;

/* Собирает промпт для картинки образа по слоям (порядок значения не важен —
   каждый слой уже готовая фраза). skipKeys — слои, не влияющие на внешний вид
   картинки (Макияж/Уход подписываются текстом отдельно, но не рисуются заново
   на иллюстрации в полный рост, чтобы не путать модель — на таком расстоянии
   макияж всё равно не разглядеть).

   2026-09-13 — "Причёска" убрана из этого списка (раньше пропускалась вместе
   с макияжем): просили, чтобы на картинке было видно лицо и рекомендованную
   причёску, а не только силуэт в полный рост. См. FULL_LENGTH ниже — там же
   добавлено требование не обрезать и не отворачивать лицо.

   2026-09-15 — "Обувь" и "Аксессуары" добавлены в список пропускаемых: разбор
   реальных тестов показал, что почти все несовпадения цвета/типа вещи с
   картинкой были именно у обуви и украшений ("бирюзовые туфли-лодочки" → на
   картинке зелёные ботинки, "эспадрильи" → не то). Это мелкие детали на
   фото в полный рост — их всё равно не разглядеть точно, а вот одежда
   (верх/низ/верхняя одежда), которая и формирует силуэт образа, столько
   несовпадений не давала. Решение пользователя: пусть картинка честно
   отвечает за силуэт+лицо+причёску, а обувь/аксессуары остаются текстом
   в карточке результата — как и раньше было с макияжем.

   Каждый слой и fit урезаются ЗАРАНЕЕ (а не полагаемся на слепую обрезку
   всего промпта по PROMPT_MAX_LENGTH в конце) — иначе при длинном описании
   слоя (например, вставленное GigaChat-описание вещи из другого экрана)
   обрезка на границе лимита могла бы съесть инструкцию про телосложение или
   "без текста на картинке" в конце промпта, а не сам длинный слой. */
function buildLookImagePrompt(layers, fit, skipKeys, gender, realKey) {
  var skip = skipKeys || ['Макияж', 'Уход', 'Обувь', 'Аксессуары'];
  var realItem = null;
  var rest = [];
  Object.keys(layers || {}).forEach(function (k) {
    if (skip.indexOf(k) !== -1) return;
    var raw = layers[k];
    var stripped = stripQuotedText(stripItemWrapper(raw));
    if (!stripped) return;
    /* realKey — явный ответ GigaChat ("Реальная вещь: <слой>", см. api/compose-look.js), какой из
       слоёв на самом деле снят с фото, а не придуман стилистом. Раньше здесь была только проверка
       ITEM_WRAPPER_RE — обёртки, которую ставит js/wizard.js для другого, статичного визарда; для
       текущего фото-потока (api/compose-look.js) ни один слой в неё никогда не завёрнут, так что
       "главная вещь" не выделялась вообще и рисовалась наравне с придуманными слоями. */
    if (k === realKey) {
      if (!realItem) realItem = stripped;
    } else if (ITEM_WRAPPER_RE.test(String(raw || '').trim())) {
      if (!realItem) realItem = stripped; // «Низ» дублирует «Верх» для платья — не повторять дважды
    } else {
      rest.push({ key: k, value: stripped });
    }
  });

  /* Порядок слоёв в rest раньше был просто порядком LAYER_KEYS (Верх, Низ,
     Верхняя одежда, ...) — при жадном заполнении бюджета (см. fitItems ниже)
     это могло отдать весь бюджет "Верх", а "Низ" не влезал вообще. Реальный
     тест поймал именно это: реальная вещь — куртка (realKey="Верхняя
     одежда"), в rest остались "Верх"/"Низ"/"Причёска" — "Верх" съел бюджет
     первым, "Низ" (юбка) пропал целиком, картинка нарисовала голые ноги без
     юбки вместо описанной "Джинсовая юбка, длина миди". "Низ" и "Верхняя
     одежда" формируют силуэт образа (см. решение 2026-09-15 про обувь/
     аксессуары выше) — они и должны занимать бюджет первыми. */
  var LAYER_PRIORITY = { 'Низ': 0, 'Верхняя одежда': 1, 'Верх': 2 };
  rest.sort(function (a, b) {
    var pa = LAYER_PRIORITY[a.key] != null ? LAYER_PRIORITY[a.key] : 3;
    var pb = LAYER_PRIORITY[b.key] != null ? LAYER_PRIORITY[b.key] : 3;
    return pa - pb;
  });
  rest = rest.map(function (r) { return r.value; });

  /* Телосложение — глобальный признак всей картинки, а не детали кроя, ставим
     его В НАЧАЛЕ промпта сразу после формата кадра: генеративные модели
     заметнее следуют указаниям ближе к началу текста, а тег "не худая
     модель" в самом конце (как было раньше) слишком легко перевешивается
     общим fashion-lookbook стилем, который по умолчанию тяготеет к типовой
     стройной модели. */
  var bodyPhrase = fit ? sizeToBodyPhrase(extractClothingSize(fit)) : null;
  /* Без явного указания пола YandexART почти всегда рисует женщину — и это
     ломает мужские образы (визард для "мужа"/"сына", "Образ по фото" с
     мужчиной на снимке). Проговариваем пол явно в обоих направлениях, а не
     только для male, чтобы не полагаться на дефолт модели. */
  var genderPhrase = gender === 'male' ? 'Модель — мужчина.' : gender === 'female' ? 'Модель — женщина.' : '';
  /* 2026-09-13 — модель регулярно обрезала кадр СВЕРХУ (голова/лицо не влезали,
     хотя обувь снизу оставалась на месте), несмотря на "от макушки до обуви,
     ничего не обрезано" — судя по всему, фраза читалась как общее пожелание
     "в полный рост", а не как явный запрет резать именно верх кадра. Прямая
     формулировка "кадр не обрезает голову сверху" — короткая специально:
     первая, более развёрнутая версия ("над макушкой есть отступ, голова и
     лицо видны целиком и не обрезаны сверху...") в паре с длинным описанием
     вещи и максимальным телосложением/полом съедала весь бюджет промпта — в
     худшем случае (мужчина, самый крупный размер) для самой вещи с фото не
     оставалось места вообще. Короче и по-прежнему явно про верх/низ кадра.

     2026-09-16 — обратная проблема: несколько живых тестов подряд кадр
     обрезался СНИЗУ (ноги от колена и обувь не влезали в кадр), несмотря на
     ту же самую фразу. "Весь силуэт" заменено на "в полный рост" — это
     устойчивый фотографический термин, которым в реальности подписывают
     фото на стоках (в отличие от "весь силуэт", это не общее описание, а
     конкретный термин композиции кадра) — может считываться моделью
     надёжнее. Разница в длину минимальная (+3 символа), бюджет под
     Низ/Верхняя одежда не страдает.

     Не помогло — два следующих живых теста подряд снова обрезаны по
     бедро/колено. По той же логике, что чинили обрезку СВЕРХУ 2026-09-13
     (общая фраза не читалась как явный запрет резать конкретно верх кадра,
     помогла прямая формулировка про голову) — добавлена прямая формулировка
     про конкретное место обрезки снизу: "ноги не срезаны по колено".
     Подтверждено, что модель в принципе умеет рисовать полный рост без
     обрезки на этом же пайплайне (пользователь показал реальные образы из
     своего личного кабинета без обрезки) — то есть это не жёсткое
     ограничение модели, а такая же случайная невыполняемость инструкции,
     как и с телосложением выше, и есть шанс, что более прямая формулировка
     поможет, как это уже сработало для головы.

     Тоже не помогло. Догадка пользователя, которая выглядит правдоподобно:
     с 2026-09-15 "Обувь" исключена из skip-листа для КАРТИНКИ (см. ниже) —
     ни в одном слое промпта больше нет описания обуви вообще, хотя эта же
     рамочная фраза просит "не обрезать обувь снизу". Модели, возможно,
     буквально нечего рисовать внизу кадра — ни одной пары обуви ни в одной
     части промпта, только абстрактное упоминание "обувь снизу" без вещи.
     Не возвращаем реальное описание обуви в промпт (решение 2026-09-15 было
     про несовпадения цвета/типа — та причина не изменилась), а даём модели
     прямое разрешение придумать любую подходящую обувь самой — не для
     точности, а чтобы внизу кадра было что рисовать физически.

     2026-09-16, ещё раз: обе добавки ("ноги не срезаны по колено" + "придумай
     обувь") жили в ГАРАНТИРОВАННОЙ части (FULL_LENGTH) — и реальный тест
     показал цену: они настолько раздули гарантированный блок, что для
     "Низ"/"Верхняя одежда" не осталось бюджета ВООБЩЕ (пустой "Остальной
     образ", картинка нарисовала футболку как платье от себя — просто
     нечем было её поправить). С этого момента у обрезки кадра есть ДРУГАЯ,
     отдельная страховка — автопроверка картинки с перегенерацией (см.
     api/compose-look.js, imageNeedsRetry, до 3 попыток) — а у точности кроя
     такой страховки нет вообще. Раз есть более надёжный резервный механизм
     именно для кадра, сами эти две фразы понижены до необязательных
     подсказок (см. notes ниже, добавляются только если осталось место,
     после кроя и телосложения) — лучше без них, чем без юбки/пальто в
     тексте. */
  var FULL_LENGTH = 'Fashion-лукбук, в полный рост от макушки до обуви: кадр не обрезает голову сверху и обувь ' +
    'снизу. Лицо анфас, причёска видна' +
    (genderPhrase ? '. ' + genderPhrase : '') +
    (bodyPhrase ? '. ОБЯЗАТЕЛЬНО: у модели ' + bodyPhrase + ', не стройная модель' : '');
  var kneeNote = ' Ноги не срезаны по колено.';
  var shoeNote = ' Придумай подходящую обувь, она видна целиком.';

  /* YandexART регулярно рисует куртку/жакет как безрукавку/жилет, даже когда
     текст слоя однозначно называет её курткой (проверено: GigaChat пишет
     "куртка джинсовая" правильно, картинка всё равно без рукавов) — модель,
     видимо, тянет короткую длину "ниже талии/оверсайз" в сторону жилета.
     Явно и отдельно проговариваем длину рукава, только когда это реально
     нужно (не раздуваем промпт на каждый образ). */
  var outerLayer = (layers && layers['Верхняя одежда']) || '';
  var sleeveNote = (/куртк|жакет|пиджак|кардиган/i.test(outerLayer) && !/жилет|безрукав/i.test(outerLayer))
    ? ' Куртка/жакет — обязательно с длинными рукавами, не жилет и не безрукавка.'
    : '';

  /* Похожая история, что и с курткой-жилетом: если "Верх" — обычная короткая вещь (футболка/
     рубашка/топ и т.п.), а не платье/сарафан, YandexART всё равно норовит нарисовать её длинной,
     в пол, будто платье — и тогда отдельно описанный "Низ" (брюки/юбка/шорты) на картинке просто
     не появляется, хотя в тексте образа он есть. Явно запрещаем это, только когда есть отдельный
     "Низ" и сам "Верх" не выглядит как платье/сарафан/комбинезон (иначе это ломало бы легитимный
     случай платья без отдельного низа). */
  var topLayer = (layers && layers['Верх']) || '';
  var bottomLayer = (layers && layers['Низ']) || '';
  var dressNote = (topLayer && bottomLayer && !/плать|сарафан|комбинезон|туник/i.test(topLayer))
    ? ' "Верх" не платье — короткая вещь, низ виден на ногах.'
    : '';

  var noTextNote = ' Без текста на картинке.';

  /* 2026-09-16 — сначала добавил повтор фразы про фигуру в самом конце
     промпта КАК ГАРАНТИРОВАННЫЙ (наравне с "без текста"), но это оказалось
     ровно той же ошибкой, что чинили 2026-09-15 с sleeveNote/dressNote:
     реальный тест (Андрей, размер 62, рубашка+джинсы+кардиган) показал, что
     "Остальной образ" ужался до ПУСТОТЫ — ни джинсов, ни кардигана в
     промпте не осталось вообще, хотя в текстовом описании образа оба слоя
     были. Повтор про фигуру важен, но НЕ важнее самого силуэта одежды —
     возвращаю его в разряд необязательных подсказок (как sleeveNote/
     dressNote), просто с более высоким приоритетом среди них (добавляется
     первым, если место осталось). Гарантированным остаётся только "без
     текста на картинке" — он короткий и почти ничего не стоит бюджету. */
  var bodyRepeatNote = bodyPhrase ? ' Повторяю: фигура — ' + bodyPhraseShort(bodyPhrase) + ', не худая.' : '';

  /* 2026-09-15 — раньше sleeveNote/dressNote были в "tail" и резервировались
     ПЕРЕД "Остальной образ" — из-за этого при их одновременном срабатывании
     (обычное дело: почти в каждом образе есть куртка + короткий верх) сам
     список "Остальной образ" (после исключения обуви/аксессуаров это теперь
     ВСЕГДА только "Низ" и "Верхняя одежда" — то есть силуэт образа, самое
     важное) мог обрезаться до одного слова или вообще пропадать. Модель
     рисовала голые ноги без низа и путала цвет куртки — именно то, что
     sleeveNote/dressNote должны были предотвратить.
     Порядок приоритета (от важного к необязательному): рамка кадра →
     главная вещь → "Низ"/"Верхняя одежда" (теперь это и есть весь список
     рекомендаций, раз обувь/аксессуары не рисуем) → bodyRepeatNote/
     sleeveNote/dressNote (подсказки-подстраховки, не критичны сами по
     себе) → "без текста". Подсказки добавляются, только если после
     гарантированного контента ещё остаётся место — лучше без них, чем без
     самой куртки/низа в тексте. */
  var guaranteedBudget = PROMPT_MAX_LENGTH - FULL_LENGTH.length - noTextNote.length;

  /* Раньше список вещей склеивался в одну строку и резался по символам
     (truncate(joined, avail)) — при нехватке места это могло обрезать
     ПОСЕРЕДИНЕ списка, целиком съедая одну из вещей (например "Низ") и
     оставляя висящую запятую, а следующие вещи после неё пропадали молча.
     2026-09-16 — реальный тест поймал это на "Джинсовая юбка, длина миди":
     из-за более тесного бюджета (framing-фразы того же дня) вещь исчезла из
     промпта целиком, картинка нарисовала голые ноги без юбки. Список вещей
     уже идёт в порядке важности (см. LAYER_KEYS выше — "Низ"/"Верхняя
     одежда" раньше "Причёска"), поэтому жадное добавление по одной вещи,
     пока помещается, само расставляет приоритет правильно — просто нужно
     не резать вещи по живому, а либо включать целиком, либо не включать. */
  function fitItems(items, avail) {
    var kept = [];
    var used = 0;
    for (var i = 0; i < items.length; i++) {
      var sep = kept.length ? 2 : 0; // ", "
      if (used + sep + items[i].length > avail) break;
      kept.push(items[i]);
      used += sep + items[i].length;
    }
    return kept.join(', ');
  }

  var middle;
  if (realItem) {
    var itemPrefix = '. Главная вещь на картинке, нарисуй именно её: ';
    var itemMax = Math.min(REAL_ITEM_MAX, Math.max(0, guaranteedBudget - itemPrefix.length - 1));
    middle = itemPrefix + truncate(realItem, itemMax) + '.';

    var restBudget = guaranteedBudget - middle.length;
    if (rest.length && restBudget > 20) {
      var restPrefix = ' Остальной образ вокруг неё: ';
      var items = rest.map(function (s) { return truncate(s, TEMPLATE_LAYER_MAX_WITH_ITEM); });
      var avail = restBudget - restPrefix.length - 1; // -1 на завершающую точку
      var joined = fitItems(items, Math.max(0, avail));
      if (joined) middle += restPrefix + joined + '.';
    }
  } else {
    var parts = rest.map(function (s) { return truncate(s, TEMPLATE_LAYER_MAX_ALONE); });
    var availAlone = Math.max(0, guaranteedBudget - 2); // ': ' + '.'
    var partsJoined = fitItems(parts, availAlone);
    middle = ': ' + partsJoined + '.';
  }
  if (middle.length > guaranteedBudget) middle = middle.slice(0, guaranteedBudget);

  /* Приоритет среди необязательных подсказок, от самого важного к самому
     необязательному:
     1) sleeveNote/dressNote — реальный крой вещи (жакет ≠ жилет, "Верх" ≠
        платье). Для этого нет отдельной страховки нигде в системе.
     2) bodyRepeatNote — повтор фигуры. У фигуры уже есть гарантированное
        упоминание в FULL_LENGTH выше, а автопроверка картинки (см.
        api/compose-look.js) тоже отдельно проверяет размер — это бонус
        поверх двух других механизмов.
     3) kneeNote/shoeNote — про обрезку кадра снизу. Тоже есть отдельная
        страховка (та же автопроверка + до 3 генераций) — самый низкий
        приоритет из всех подсказок, добавляются последними. */
  var leftover = PROMPT_MAX_LENGTH - FULL_LENGTH.length - middle.length - noTextNote.length;
  var notes = '';
  if (sleeveNote && notes.length + sleeveNote.length <= leftover) notes += sleeveNote;
  if (dressNote && notes.length + dressNote.length <= leftover) notes += dressNote;
  if (bodyRepeatNote && notes.length + bodyRepeatNote.length <= leftover) notes += bodyRepeatNote;
  if (kneeNote && notes.length + kneeNote.length <= leftover) notes += kneeNote;
  if (shoeNote && notes.length + shoeNote.length <= leftover) notes += shoeNote;

  return FULL_LENGTH + middle + notes + noTextNote;
}

module.exports = {
  generateLookImage: generateLookImage,
  buildLookImagePrompt: buildLookImagePrompt,
  sizeToBodyPhrase: sizeToBodyPhrase,
  extractClothingSize: extractClothingSize
};
